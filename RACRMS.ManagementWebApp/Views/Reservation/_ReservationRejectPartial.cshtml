@model ReservationDTO

<script>
        $(() => {

            $(document).ready(() => {
                $('#modal_form_horizontal').modal('show');

                $('#modal_form_horizontal').on('hidden.bs.modal', (e) => window.location.href = '@Url.Action("Cancel")');

                $('.form-horizontal').validate({
                    ignore: 'input[type=hidden], .select2-search__field', // ignore hidden fields
                    errorClass: 'validation-invalid-label',
                    successClass: 'validation-valid-label',
                    validClass: 'validation-valid-label',
                    highlight: function (element, errorClass) {
                        $(element).removeClass(errorClass);
                    },
                    unhighlight: function (element, errorClass) {
                        $(element).removeClass(errorClass);
                    },
                    rules: {
                        Name: {
                            required: true,
                            maxlength: 15
                        }
                    },
                    messages: {
                        Name: {
                            required: "Bu alan boş bırakılamaz.",
                            maxlength: "En fazla 25 karakter girişi yapılabilir."
                        }
                    }
                });
            });

        })
</script>

<!-- Horizontal form modal -->
<div id="modal_form_horizontal" class="modal fade" data-keyboard="false" data-backdrop="static" tabindex="-1">
    <div class="modal-dialog">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title"><i class="icon-subtract mr-2"></i>Rezervasyon Reddet</h5>
                <button type="button" class="close" data-dismiss="modal">&times;</button>
            </div>

            @using (Html.BeginForm("ReservationReject", "Reservation", FormMethod.Post, new { @class = "form-horizontal" }))
            {
                <div class="modal-body">
                    @Html.HiddenFor(x => x.Id)
                    <div class="form-group row p-2">
                        <div class="col-sm-12">
                            @Html.LabelFor(x => x.ReservationCode)
                            @Html.TextBoxFor(x => x.ReservationCode, new { placeholder = "Kayıt giriniz...", @class = "form-control", disabled = "disabled" })
                        </div>
                    </div>
                    <div class="form-group row p-2">
                        <div class="col-sm-12">
                            @Html.LabelFor(x => x.PlateNumber)
                            @Html.TextBoxFor(x => x.PlateNumber, new { placeholder = "Kayıt giriniz...", @class = "form-control", disabled = "disabled" })
                        </div>
                    </div>
                    <div class="form-group row p-2">
                        <div class="col-sm-6 mb-2 mb-sm-0 mb-md-0 mb-lg-0 mb-xl-0">
                            @Html.LabelFor(x => x.CarBrandName)
                            @Html.TextBoxFor(x => x.CarBrandName, new { placeholder = "Kayıt giriniz...", @class = "form-control", disabled = "disabled" })
                        </div>
                        <div class="col-sm-6">
                            @Html.LabelFor(x => x.CarModelName)
                            @Html.TextBoxFor(x => x.CarModelName, new { placeholder = "Kayıt giriniz...", @class = "form-control", disabled = "disabled" })
                        </div>
                    </div>
                    <div class="form-group row p-2">
                        <div class="col-sm-6 mb-2 mb-sm-0 mb-md-0 mb-lg-0 mb-xl-0">
                            @Html.LabelFor(x => x.CustomerName)
                            @Html.TextBoxFor(x => x.CustomerName, new { placeholder = "Kayıt giriniz...", @class = "form-control", disabled = "disabled" })
                        </div>
                        <div class="col-sm-6">
                            @Html.LabelFor(x => x.CustomerSurname)
                            @Html.TextBoxFor(x => x.CustomerSurname, new { placeholder = "Kayıt giriniz...", @class = "form-control", disabled = "disabled" })
                        </div>
                    </div>
                    <div class="form-group row p-2">
                        <div class="col-sm-6 mb-2 mb-sm-0 mb-md-0 mb-lg-0 mb-xl-0">
                            @Html.LabelFor(x => x.StartDate)
                            @Html.TextBoxFor(x => x.StartDate, "{0:dd.MM.yyyy HH:mm:ss}", new { placeholder = "Kayıt giriniz...", @class = "form-control", disabled = "disabled" })
                        </div>
                        <div class="col-sm-6">
                            @Html.LabelFor(x => x.EndDate)
                            @Html.TextBoxFor(x => x.EndDate, "{0:dd.MM.yyyy HH:mm:ss}", new { placeholder = "Kayıt giriniz...", @class = "form-control", disabled = "disabled" })
                        </div>
                    </div>
                    <div class="form-group row p-2">
                        <div class="col-sm-12">
                            @Html.LabelFor(x => x.TotalPrice)
                            @Html.TextBoxFor(x => x.TotalPrice, "{0:#.## ₺}", new { placeholder = "Kayıt giriniz...", @class = "form-control", disabled = "disabled" })
                        </div>
                    </div>
                </div>

                <div class="modal-footer">
                    <a href="@Url.Action("Cancel")" class="btn btn-outline-danger" data-dismiss="modal"><i class="icon-cancel-square2 mr-2"></i> İptal</a>
                    <button type="submit" class="btn btn-success"><i class="icon-subtract mr-2"></i> Reddet</button>
                </div>
            }
        </div>
    </div>
</div>
<!-- /horizontal form modal -->